services:
  - type: web
    name: chat-server
    env: node
    rootDir: cv/chat-server
    buildCommand: npm install
    startCommand: npm start
    autoDeploy: true
    healthCheckPath: /health
    envVars:
      - key: ALLOWED_ORIGIN
        value: https://cv.theodorenelson.co.za
